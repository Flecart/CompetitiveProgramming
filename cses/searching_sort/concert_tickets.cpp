#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
typedef pair<int, int> pii;

int main()
{
    int n, m;
    cin >> n >> m;
    int arr[n];
    int cust[m];

    for (int i = 0; i < n; i++)  {
        cin >> arr[i];
    }

    for (int i = 0; i < m; i++) {
        cin >> cust[i];
    }

    
    return 0;
}